apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "example-angular-order.fullname" . }}-test-connection"
  labels:
    {{- include "example-angular-order.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "example-angular-order.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
